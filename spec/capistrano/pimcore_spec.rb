require 'spec_helper'

describe Capistrano::Pimcore do
  it 'has a version number' do
    expect(Capistrano::Pimcore::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
